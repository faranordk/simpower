% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_event.R
\name{power_event}
\alias{power_event}
\title{Power and MDE for an event-study design}
\usage{
power_event(
  data,
  y,
  id,
  time,
  treat = NULL,
  cohort = NULL,
  controls = NULL,
  shape = c("constant", "increasing", "decreasing"),
  horizons = NULL,
  leads = NULL,
  n_units = NULL,
  n_treated = NULL,
  reps = 1000,
  emax = NULL,
  step = NULL,
  alpha = 0.05,
  alternative = c("greater", "two.sided", "less"),
  crit_method = c("empirical", "normal"),
  power_targets = c(0.8, 0.9),
  seed = 5000
)
}
\arguments{
\item{data}{A data frame in long (panel) format.}

\item{y}{Column name (string) of the outcome variable.}

\item{id}{Column name of the unit id (panel variable).}

\item{time}{Column name of the time variable.}

\item{treat}{Column name of an absorbing 0/1 treatment indicator. Optional if
\code{cohort} is supplied.}

\item{cohort}{Optional column name giving each unit's adoption time (on the
same scale as \code{time}); never-treated units take \code{NA}/\code{Inf}.}

\item{controls}{Optional character vector of control-variable column names.
Factors/characters are expanded to dummies. Controls are partialled out in
every simulated fit.}

\item{shape}{Assumed shape of the true effect path over post-treatment
horizons: \code{"constant"}, \code{"increasing"}, or \code{"decreasing"}.}

\item{horizons}{Maximum post-treatment horizon to include -- i.e. how many
periods after treatment to model (default: as many as the data support,
capped at 20).}

\item{leads}{Number of pre-treatment leads to include as placebo terms
(default: up to 5). Horizon \code{-1} is always the omitted baseline.}

\item{n_units}{Optional. Total number of units to draw for each simulated
sample. When \code{NULL} the full sample is used.}

\item{n_treated}{Number of eventually-treated units to draw per simulated
sample (see \code{\link[=power_twfe]{power_twfe()}}).}

\item{reps}{Number of simulations (curve smoothness; does not affect bias).}

\item{emax, step}{Optional largest effect size and grid step on the power
curve. When \code{NULL} they are chosen automatically from the null SE scale.}

\item{alpha}{Significance level.}

\item{alternative}{Test direction: \code{"greater"} (one-sided, the default and
the paper's convention), \code{"less"}, or \code{"two.sided"}.}

\item{crit_method}{How the critical value for the test is obtained.
\code{"empirical"} (default) reads it off the simulated null distribution of the
t-statistics, giving the test exactly the requested size even under
clustered / small-sample standard errors. \code{"normal"} uses the closed-form
Gaussian quantile (the previous behaviour).}

\item{power_targets}{Power levels at which to report the MDE.}

\item{seed}{Random seed (for reproducibility).}
}
\value{
An object of class \code{"simpower"} (design \code{"event"}). Its \code{extras$horizon}
element holds the per-horizon MDE table.
}
\description{
Simulation-based power analysis for a dynamic (event-study) treatment effect
in a two-way fixed-effects panel. Two objects are produced:
}
\details{
\itemize{
\item an \strong{overall} power curve for a single shaped-exposure coefficient. The
assumed effect path across post-treatment horizons is set by \code{shape}
(\code{"constant"}, \code{"increasing"}, or \code{"decreasing"}), normalised so the peak
horizon has weight 1; the overall MDE is therefore expressed in units of
the \strong{peak} effect.
\item a \strong{per-horizon} minimum detectable effect: for each post-treatment
horizon \eqn{h}, the smallest effect the design could detect at that
horizon on its own, read off the dynamic event-study regression. This is
reported both as the raw effect (\code{mde}) and, taking the assumed \code{shape}
into account, in units of the peak effect (\code{mde_peak = mde / weight}); the
per-horizon plot uses the latter.
}

Treatment timing is taken from an absorbing 0/1 indicator \code{treat} (once on it
stays on) or, alternatively, from a \code{cohort} column giving each unit's
adoption time. Relative event time is \code{time - adoption}; horizon \code{-1} is the
omitted reference period.
}
\examples{
set.seed(1)
df <- sim_event(n_units = 80, n_time = 10, adopt = 6)
pw <- power_event(df, y = "y", id = "id", time = "t", treat = "d",
                  shape = "increasing", reps = 150)
pw
}
