% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_iv.R
\name{power_iv}
\alias{power_iv}
\title{Power and MDE for an instrumental-variables (2SLS) design}
\usage{
power_iv(
  data,
  y,
  d,
  z = NULL,
  id = NULL,
  time = NULL,
  controls = NULL,
  n_units = NULL,
  n_treated = NULL,
  L = NULL,
  rho = c(0.1, 0.3, 0.5),
  reps = 1000,
  emax = NULL,
  step = NULL,
  alpha = 0.05,
  alternative = c("greater", "two.sided", "less"),
  crit_method = c("empirical", "normal"),
  power_targets = c(0.8, 0.9),
  seed = 5000,
  het = NULL
)
}
\arguments{
\item{data}{A data frame in long (panel) format.}

\item{y}{Column name (string) of the outcome variable.}

\item{d}{Column name of the endogenous regressor whose coefficient is tested.}

\item{z}{Column name(s) of the excluded instrument(s).}

\item{id}{Optional column name of a clustering / panel unit id. If supplied
(with \code{time}) the outcome is recombined by donor series to preserve
within-unit autocorrelation and SEs are clustered by \code{id}; otherwise a
cross-sectional residual-permutation null with HC1 SEs is used.}

\item{time}{Optional time column (only used, with \code{id}, for panel-style
recombination).}

\item{controls}{Optional character vector of control-variable column names.
Factors/characters are expanded to dummies. Controls are partialled out in
every simulated fit, and the recombination preserves their predicted
component of the outcome (only the control-purged residual is recombined
across units), so the simulated SEs -- and the MDE -- reflect the
efficiency gain from controls that predict the outcome.}

\item{n_units, n_treated}{Number of units and treated units (\code{d == 1}) to draw
per simulated sample. Units are clusters when \code{id} is given, otherwise
individual observations.}

\item{L}{Optional column name switching on \strong{hypothetical-instrument mode}
(supply either \code{z} or \code{L}, not both). \code{L} is any variable whose
distributional character you want the imagined instrument to inherit. On
every simulation rep the \code{L} series is randomly permuted -- breaking its
correlation with both the treatment and the outcome -- and a synthetic
instrument is built as \eqn{z_r = r\,\tilde X + \sqrt{1-r^2}\,\tilde
  L_\perp}, where \eqn{\tilde X} is the standardised treatment and
\eqn{\tilde L_\perp} the permuted \code{L} residualised on the treatment and
standardised. By construction the \emph{in-sample Pearson correlation between
the instrument and the treatment is exactly} \code{r} on every rep, so the
resulting curves isolate what instrument strength alone does to power.
One power curve and one MDE row are returned per value of \code{rho}.}

\item{rho}{Numeric vector of target first-stage correlations (Pearson r
between the synthetic instrument and the treatment), each strictly
between 0 and 1. Only used in hypothetical-instrument mode. Default
\code{c(0.1, 0.3, 0.5)}.}

\item{reps}{Number of simulations (curve smoothness; does not affect bias).}

\item{emax, step}{Optional largest effect size and grid step on the power
curve. When \code{NULL} they are chosen automatically from the null SE scale.}

\item{alpha}{Significance level.}

\item{alternative}{Test direction: \code{"greater"} (one-sided, the default and
the paper's convention), \code{"less"}, or \code{"two.sided"}.}

\item{crit_method}{How the critical value for the test is obtained.
\code{"empirical"} (default) reads it off the simulated null distribution of the
t-statistics, giving the test exactly the requested size even under
clustered / small-sample standard errors. \code{"normal"} uses the closed-form
Gaussian quantile (the previous behaviour).}

\item{power_targets}{Power levels at which to report the MDE.}

\item{seed}{Random seed (for reproducibility).}

\item{het}{Optional \strong{heterogeneous-effects} specification (default \code{NULL}
= the usual constant effect). Real effects rarely hit every unit equally,
and heterogeneity costs power; \code{het} quantifies that cost. Supply either a
numeric vector interpreted as an equal-probability set of per-unit effect
multipliers -- e.g. \code{het = c(0, 2)}: the effect is absent in half the
units and doubled in the other half -- or a \verb{function(n)} returning \code{n}
multipliers (e.g. \code{function(n) rlnorm(n, -0.32, 0.8)}). Multiplier
vectors are normalised to mean one, so the effect axis remains the
\strong{average} effect and curves are directly comparable to the constant
case. Each rep draws fresh multipliers per unit (per cluster in IV/RDD;
per observation when there is no id) on an isolated RNG stream, so the
simulated null draws are bit-identical to a run without \code{het} (common
random numbers). Because all estimators are linear in the outcome and all
SEs are sandwiches, the heterogeneous injection is evaluated \emph{exactly} --
the coefficient shifts by \code{e * lambda} and the variance is quadratic in
\code{e} -- with no refitting across effect sizes. The returned \verb{$power} and
\verb{$mde} reflect the heterogeneous effect; \code{extras$het} stores the
constant-effect curve and MDE for comparison. In \code{power_event()} the
heterogeneity applies to the overall shaped-exposure coefficient (the
per-horizon table keeps the constant interpretation); realized test size
is unchanged (at effect 0 the two setups coincide).}
}
\value{
An object of class \code{"simpower"} (design \code{"iv"}). \code{extras} holds the
real-data 2SLS estimate and the first-stage F statistic. In
hypothetical-instrument mode \verb{$power} holds one curve per \code{rho} (long
format, with a \code{rho} column), \verb{$mde} is a data frame with one row per
\code{rho}, and \code{extras$first_stage_F} reports the implied first-stage F at
each \code{rho} (deterministic given the sample size, because the in-sample
correlation is exact by construction).
}
\description{
Simulation-based power analysis for the coefficient on an endogenous
regressor \code{d} instrumented by \code{z}, estimated by two-stage least squares.
Because 2SLS is linear in the outcome, the analytic effect-size sweep is
exact: adding a true effect \eqn{\tau} shifts the 2SLS coefficient on \code{d} by
exactly \eqn{\tau} and leaves the SE unchanged. Instrument strength (the
first stage) is preserved because the real \code{z} and \code{d} are used in every
simulated fit --- weak instruments show up as low power automatically.
}
\examples{
set.seed(1)
df <- sim_iv(n = 800, strength = 0.4)
pw <- power_iv(df, y = "y", d = "d", z = "z", reps = 200)
pw

## hypothetical instrument: how would power look with cor(z, d) = .1/.3/.5?
df$candidate <- rnorm(nrow(df))
ph <- power_iv(df, y = "y", d = "d", L = "candidate",
               rho = c(0.1, 0.3, 0.5), reps = 200)
ph
}
