% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan.R
\name{plan_mde}
\alias{plan_mde}
\title{Trace the MDE across sample sizes (design planning)}
\usage{
plan_mde(
  object,
  n_units = NULL,
  n_treated = NULL,
  target = 0.8,
  standardized = FALSE,
  reps = NULL,
  plot = TRUE,
  envir = parent.frame()
)
}
\arguments{
\item{object}{A fitted \code{simpower} object (from any of the \verb{power_*}
functions). Its stored call is re-evaluated with the sample-size arguments
replaced.}

\item{n_units, n_treated}{Vectors of sample sizes to sweep. Supply either or
both; if both are given they are recycled to a common length and varied in
lockstep (pairs of total/treated counts).}

\item{target}{Power level at which to read the MDE.}

\item{standardized}{If \code{TRUE}, report the MDE in within-SD-of-outcome units.}

\item{reps}{Optional override for the number of simulations per point (useful
to keep the sweep fast).}

\item{plot}{If \code{TRUE}, the returned object prints as an MDE-vs-sample-size
plot.}

\item{envir}{Environment in which to re-evaluate the original call (defaults
to the caller); must be able to see the original \code{data}.}
}
\value{
An object of class \code{"simpower_plan"}: a list with \code{table} (a data
frame of sample size and MDE) and \code{plot} (a ggplot).
}
\description{
Re-runs a fitted \link{simpower} analysis over a grid of \code{n_units} and/or
\code{n_treated} values and returns how the minimum detectable effect changes with
sample size --- the core question in ex-ante design planning ("how many units
do I need to detect an effect of size X?").
}
\examples{
set.seed(1)
df <- sim_panel(120, 8)
pw <- power_twfe(df, "y", "x", "id", "t", reps = 100)
pl <- plan_mde(pw, n_units = c(30, 60, 90, 120), reps = 100)
pl$table
}
