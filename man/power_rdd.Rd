% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_rdd.R
\name{power_rdd}
\alias{power_rdd}
\title{Power and MDE for a sharp regression-discontinuity (RDD) design}
\usage{
power_rdd(
  data,
  y,
  run,
  cutoff = 0,
  controls = NULL,
  bandwidth = NULL,
  bw_grid = NULL,
  id = NULL,
  n_units = NULL,
  n_treated = NULL,
  reps = 1000,
  bw_reps = 300,
  emax = NULL,
  step = NULL,
  kernel = c("triangular", "uniform"),
  alpha = 0.05,
  alternative = c("greater", "two.sided", "less"),
  crit_method = c("empirical", "normal"),
  power_targets = c(0.8, 0.9),
  seed = 5000,
  use_rdrobust = TRUE
)
}
\arguments{
\item{data}{A data frame in long (panel) format.}

\item{y}{Column name (string) of the outcome variable.}

\item{run}{Column name of the running (forcing) variable.}

\item{cutoff}{Threshold value of \code{run} at which treatment switches on
(treatment = \code{run >= cutoff}).}

\item{controls}{Optional character vector of control-variable column names.
Factors/characters are expanded to dummies. Controls are partialled out in
every simulated fit.}

\item{bandwidth}{Half-width of the estimation window around the cutoff. When
\code{NULL}, an MSE-optimal bandwidth from \pkg{rdrobust} is used if available,
otherwise a rule-of-thumb value (and a message is shown).}

\item{bw_grid}{Optional vector of bandwidths for the MDE-versus-bandwidth
graph. Defaults to a grid spanning roughly half to 1.5x the main bandwidth.}

\item{id}{Optional cluster id (SEs clustered by \code{id} when supplied).}

\item{n_units, n_treated}{Number of units and treated units (\code{run >= cutoff})
drawn from the \emph{available} sample before the bandwidth window is applied.}

\item{reps}{Number of simulations (curve smoothness; does not affect bias).}

\item{bw_reps}{Number of simulations per bandwidth in the sweep (kept smaller
than \code{reps} by default to bound run time).}

\item{emax, step}{Optional largest effect size and grid step on the power
curve. When \code{NULL} they are chosen automatically from the null SE scale.}

\item{kernel}{Weighting kernel for the local-linear fit: \code{"triangular"}
(default) or \code{"uniform"} (equivalent to OLS within the bandwidth window).}

\item{alpha}{Significance level.}

\item{alternative}{Test direction: \code{"greater"} (one-sided, the default and
the paper's convention), \code{"less"}, or \code{"two.sided"}.}

\item{crit_method}{How the critical value for the test is obtained.
\code{"empirical"} (default) reads it off the simulated null distribution of the
t-statistics, giving the test exactly the requested size even under
clustered / small-sample standard errors. \code{"normal"} uses the closed-form
Gaussian quantile (the previous behaviour).}

\item{power_targets}{Power levels at which to report the MDE.}

\item{seed}{Random seed (for reproducibility).}

\item{use_rdrobust}{If \code{TRUE} and \pkg{rdrobust} is installed, use it for
MSE-optimal bandwidth selection when \code{bandwidth} is \code{NULL}.}
}
\value{
An object of class \code{"simpower"} (design \code{"rdd"}). \code{extras$bandwidth}
holds the MDE-versus-bandwidth table (its \code{n_in_bw} column counts
\emph{observations} inside each window) and \code{extras$h} the main bandwidth. In
the sample-composition check, units and treated units are counted as
clusters when \code{id} is supplied and as observations otherwise; the treated
target refers to the full sample, the simulation average to what remains
inside the bandwidth window.
}
\description{
Simulation-based power analysis for the treatment jump at a cutoff in a sharp
RDD, estimated by local-linear regression with a triangular kernel. The null
is imposed by preserving the running-variable trend on each side while
permuting residuals (so there is no jump), and the RD estimator is refit on
each recombined sample. Because the local-linear estimator is linear in the
outcome, the analytic effect-size sweep is exact.
}
\details{
Besides the overall power curve at the chosen bandwidth, a second result
traces how the MDE changes as the bandwidth changes.
}
\examples{
set.seed(1)
df <- sim_rdd(n = 1500, jump = 0)
pw <- power_rdd(df, y = "y", run = "x", cutoff = 0,
                bandwidth = 0.5, reps = 200, bw_reps = 120)
pw
}
